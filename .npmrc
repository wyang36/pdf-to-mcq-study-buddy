# CopilotKit's runtime declares a peerOptional on a newer @langchain/langgraph-sdk
# than @langchain/langgraph@0.2 pulls in. It's a peer-*optional* mismatch that does
# not affect runtime, so we relax peer resolution to keep installs clean.
legacy-peer-deps=true
